---
# role: {{ role_name }}
# file: tasks/assertions.yml
# vim: ft=yaml et sts=2 sw=2 ts=2

- name: "validate OS and Ansible versions"
  assert:
    that:
      - ansible_os_family in == "Debian" and ansible_distribution_major_version == "10"
      - ansible_version.full is version('2.8', '>=')
